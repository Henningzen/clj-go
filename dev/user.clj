(ns user
  (:require
   [jansenh.clj-go.core :as clj-go :refer [clj-go]]
   [jansenh.clj-go.utilities :as utilities]
   [jansenh.clj-go.engine :as eng]
   [jansenh.clj-go.terminal-board :as term-board]
   [clojure.repl :refer :all]
   [clojure.pprint :refer [pprint]]
   [clojure.tools.namespace.repl :refer [refresh refresh-all]]))


(comment

  (utilities/numeric-board)
  (utilities/patterned-board)

  ;; --->
  )

(comment

  (clj-go)
  ;; --->
  )


